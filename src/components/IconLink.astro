---
import { Image } from "astro:assets";

interface Props extends astroHTML.JSX.AnchorHTMLAttributes {
  src: any;
}

const { src } = Astro.props;
---

<a {...Astro.props} class="py-2 px-4 bg-black text-white flex gap-1 rounded-lg">
  <Image src={src} alt={""} />
  <slot />
</a>
