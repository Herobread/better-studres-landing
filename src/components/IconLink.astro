---
import type { ImageMetadata } from "astro";

interface Props {
  image: ImageMetadata;
  href: string
}

const { href, image} = Astro.props;
---

<a href={href} class="py-2 px-4 bg-black text-white flex gap-1 rounded-lg">
  <img src={image.src} alt={""} />
  <slot />
</a>