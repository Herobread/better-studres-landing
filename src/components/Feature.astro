---
import { Image } from "astro:assets";

interface Props {
  src: any;
  header: string;
  description: string;
}

const { description, header, src } = Astro.props;
---

<div>
  <Image src={src} alt="" class="w-9 h-9" />
  <h3 class="text-xl font-bold">{header}</h3>
  <p>{description}</p>
</div>
